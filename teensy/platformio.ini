[env:teensy41]
platform  = teensy
board     = teensy41
framework = arduino

; Source is in the same directory as platformio.ini
src_dir = .

; USB serial for Teensy Loader and Serial comms
build_flags = -DUSB_SERIAL

monitor_speed = 115200
